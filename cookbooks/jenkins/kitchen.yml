---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: centos-8

suites:
  - name: default
    run_list:
      - recipe[jenkins::directories]
      - recipe[jenkins::install_java]
      - recipe[jenkins::software_repository]
    attributes:
